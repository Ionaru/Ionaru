name: npm package CI/CD

on: [workflow_call]

jobs:

  test:

    runs-on: ubuntu-latest
    uses: Ionaru/Ionaru/.github/workflows/npm-test.yaml@main

  deploy:

    needs: [ test ]
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    uses: Ionaru/Ionaru/.github/workflows/npm-deploy.yaml@main
